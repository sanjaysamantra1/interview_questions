1. How do you optimize a website’s assets?
File concatenation, file compression, CDN Hosting, offloading assets, re-organizing and refining code, etc. Have a few ready.
2. Explain web accessibility.
3. What are the three ways to reduce page load time?
Reduce image sizes, remove unnecessary widgets, and HTTP compression, put CSS at the top and script references at the bottom 
or in external files, reduce lookups, minimize redirects, caching, etc.
4. How would you implement internationalization (i18n)  (Multilingual webpage)

Q1. Explain authentication and authorisation process using JWT token and Oauth 2.0
Q2. How can handle scalability if sudden trafic got increased in web application
Q3. Implementation REST api using express js and implementation Authorization
Q4. What are the key things that need to keep in mind while implementing microservices in the terms of scalability, security, communication, monitoring etc
Q5. If we have a product table where we store different categories of product. What query i need to write to get category wise average price




 Write a program to implement lazy loading for images on a page, ensuring that images outside the viewport are loaded only when they become visible. 
  import React, { useRef, useEffect, useState } from 'react'; 
 const LazyImage = ({ src, alt }) => {
 const imgRef = useRef(); 
 const [loaded, setLoaded] = useState(false); 
 useEffect(() => { 
 const observer = new IntersectionObserver(([entry]) => 
 { 
 if (entry.isIntersecting)
  { 
  setLoaded(true); observer.disconnect(); 
  } }); 
  observer.observe(imgRef.current); 
  return () => observer.disconnect(); }, []); 
  return <img ref={imgRef} src={loaded ? src : ''} alt={alt} />; };


